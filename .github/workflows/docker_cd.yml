name: Docker CI

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      DOCKER_IMAGE_NAMESPACE: ${{ vars.DOCKER_NAMESPACE || github.repository_owner }}

    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Read VERSION file
        id: version
        run: |
          VERSION=$(cat VERSION)
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Validate Docker image namespace
        run: |
          if ! [[ "$DOCKER_IMAGE_NAMESPACE" =~ ^[a-z0-9]+([._-][a-z0-9]+)*$ ]]; then
            echo "::error::DOCKER_IMAGE_NAMESPACE must be a valid lowercase Docker Hub namespace. Set repository variable DOCKER_NAMESPACE if it differs from the GitHub owner."
            exit 1
          fi

      - name: Login to DockerHub
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Docker image
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: ./landing-page
          file: ./landing-page/Dockerfile
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE_NAMESPACE }}/ai-business-agent:${{ env.VERSION }}
            ${{ env.DOCKER_IMAGE_NAMESPACE }}/ai-business-agent:latest
          
      # Build Backend image
      - name: Build & Push AI-Agent API
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: ./agent_code
          file: ./agent_code/Dockerfile
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE_NAMESPACE }}/intelligent-business-ai-agent:${{ env.VERSION }}
            ${{ env.DOCKER_IMAGE_NAMESPACE }}/intelligent-business-ai-agent:latest
